apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: evalio
  namespace: argocd

  labels:
    app.kubernetes.io/name: evalio
    app.kubernetes.io/part-of: evalio-platform

  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  source:
    repoURL: https://github.com/Official-Krish/Evalio

    targetRevision: main

    path: ops

  destination:
    server: https://kubernetes.default.svc
    namespace: evalio

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    retry:
      limit: 5

      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
      - ApplyOutOfSyncOnly=true

  revisionHistoryLimit: 10

  info:
    - name: Environment
      value: production

    - name: Project
      value: evalio

    - name: Managed-By
      value: argocd
